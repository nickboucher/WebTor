/** op.js
 *
 * This module implements the onion proxy behavior of the Tor
 * protocol. It implements a SOCKS proxy interface in-browser and utilizes
 * a local OR in order to connect to the network.
 */
'use strict';

import crypto from './crypto';
import network from './network';

export default {
};
